import { SlicePlaceholder } from "@/components/ui/slice-placeholder";

export default function Page() {
  return <SlicePlaceholder title="Clients / Policy" note="Per-client automation mode, GSTIN/Udyam, certification policy and the kill switch." />;
}
